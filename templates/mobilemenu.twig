<div class="mod-mobilemenu">
	<div class="container-fluid">
		<div class="row">
			{% if menu_1 %}
				<div class="col-md-6 col-lg-3">
					<div class="h4 menu-title font-alt text-dark mb-1">{{ options.menu_1_titel }}</div>
					<img src="{{ theme.link }}/assets/img/line_title.svg" alt="line" class="mb-4">
					{% include "menu-mobilemenu.twig" with {'items': menu_1.get_items} %}
				</div>
			{% endif %}
			{% if menu_2 %}
					<div class="col-md-6 col-lg-3">
						<div class="h4 font-alt text-dark mb-1">{{ options.menu_2_titel }}</div>
						<img src="{{ theme.link }}/assets/img/line_title.svg" alt="line" class="mb-4">
						{% include "menu-mobilemenu.twig" with {'items': menu_2.get_items} %}
					</div>
			{% endif %}
			{% if menu_3 %}
				<div class="col-md-6 col-lg-3">
					<div class="h4 font-alt text-dark mb-1">{{ options.menu_3_titel }}</div>
					<img src="{{ theme.link }}/assets/img/line_title.svg" alt="line" class="mb-4">
					{% include "menu-mobilemenu.twig" with {'items': menu_3.get_items} %}
				</div>
			{% endif %}
			{% if menu_4 %}
				<div class="col-md-6 col-lg-3">
					<div class="h4 font-alt text-dark mb-1">{{ options.menu_4_titel }}</div>
					<img src="{{ theme.link }}/assets/img/line_title.svg" alt="line" class="mb-4">
					{% include "menu-mobilemenu.twig" with {'items': menu_4.get_items} %}
				</div>
			{% endif %}
		</div>
	</div>
</div>