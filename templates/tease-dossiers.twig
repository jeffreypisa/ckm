<a href="{{ item.link }}" target="{{ item.link.target}}" class="tease tease-dossiers mb-30 d-block">
	<div class="afbeelding-wrapper ratio-4x3">
		<div class="afbeelding bg-cover" style="background-image: url({{ item.fields.afbeelding.url }})"></div>
	</div>
	<h3 class="h4 mt-4 mb-2"> {{ item.name }} </h3>
	{{ item.description }}
	<div class="btn-link mt-3" href="{{ item.link.url }}" target="{{ item.link.target }}">Duik dieper</div>
</a>