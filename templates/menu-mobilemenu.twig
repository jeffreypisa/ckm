<ul class="nav">
	{% for item in items %}
		<li class="nav-item {{ item.classes | join(' ') }}">
			<a class="nav-link text-orange font-alt h5 mb-4" target="{{ item.target }}" href="{{ item.link }}">
				<div>{{ item.title }}</div>
			</a>
		</li>
	{% endfor %}
</ul>